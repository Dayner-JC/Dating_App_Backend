const requestChange = require("./request_change");
const verifyChange = require("./verify_change");
const updateChange = require("./update_change");

module.exports = {
  requestChange,
  verifyChange,
  updateChange,
};
